# M3.3.X Audit Report — M3.3 三图 phase re-audit

> **Auditor**: tester (M3.3.X loop)
> **Date**: 2026-05-05
> **Scope**: turn_graph + intake_graph + post_report_graph + 6 agents (Interviewer / Framework / Research / Coach / Reflection / Report)
> **Commit range**: 3b1ac52..4d4d7ef (M3.3.1.arch through M3.3.3.dev.d)
> **Gate**: must score ≥ 8/10 to unblock M3.4

---

## 1. 优势(Strengths)

- **三层 L0 节点名锁 100% 落地**: 9 个字符串字面量（turn_graph 3 + intake_graph 3 + post_report_graph 2）全部以精确拼写出现在图构建代码的 `.addNode()` 调用处，且每个图额外用 `Object.freeze(new Set([...]))` 将节点名固化为运行时常量 (`TURN_GRAPH_NODES` / `INTAKE_GRAPH_NODES` / `POST_REPORT_GRAPH_NODES`)，加上 TypeScript Literal union 类型，共 三层防御。Contract test 套件（22 + 15 + 12 = 49 tests）全绿，重命名任何节点立即触发 CI。

- **§A11 PII 零泄漏设计精良**: 8 个 AgentInputSchema 全部加 `.strict()`，拒绝任何未声明字段。ResearchAgentInputSchema N=300 LCG fuzz（seed=42）+ CoachAgentInputSchema N=50 fuzz + ReflectionAgentInputSchema N=50 fuzz 均通过，`leakCount === 0`，LLM provider 在 300 次污染尝试中从未被调用一次（`generateObject` call count = 0）。PII 字段池含 20 个高风险字段名（resume_text / candidate_email / candidate_phone / 身份证号 / passport_number 等），30% 随机字段名（randomAlpha），覆盖未知 PII 字段场景。

- **fire-and-forget 架构契约完整**: `postReportGraph.ts` 文件头注释明确标注 `void buildPostReportGraph(deps).invoke(state).catch(noop)` 调用约定；节点内部用 try/catch 将所有错误转为 `coach_error` / `reflection_error` 状态字段（存储 `err.constructor.name` 字符串），`graph.invoke` 永不抛出。并行测试通过 `Date.now()` 区间记录验证真并发（maxStart < minEnd + 20ms jitter），证明 LangGraph.js 0.2.74 从 START 的 fan-out 为真并行，非串行顺序执行。

- **错误隔离四路覆盖**: `postReportGraph.parallel.test.ts` 覆盖 Case A（coach throws）/ Case B（reflection throws）/ Case C（both throw）/ ZodError swallow（coach_input 含 PII 字段 resume_text），4 个路径全部 `threw === false`，graph.invoke 均正常返回，错误路由精准落在对应的 `_error` 字段。

- **数据契约只增不改严守**: `ReportAgentOutputSchema`（持久层）保留完整 v3.1 fallback 字段 `pass_probability` (int 0-100) / `summary` / `reasons` / `next_actions`；`ReportVerdictSchema`（`["strong","solid","mixed","weak"]`）完整保留。新增 v3.4 字段 `pass_likelihood` / `dimensions` / `round_reviews_v2` / `next_actions_v2` / `ai_verdict` / `overall_score` 以 nullable/optional 形式叠加，不删改旧字段。`git log` 显示 `reports.ts` 仅 2 次提交（初始 port + report agent 扩展），无删除记录。

- **§C3 秘密不过 Bridge 严守**: 所有 6 个 agent 实现文件（coach/index.ts / reflection/index.ts / report/index.ts / research/index.ts / framework/index.ts / interviewer/index.ts）及所有 3 个 graph 文件均无实际的 `process.env` / `getApiKey` / `keychain.read` 调用，仅有注释层标注（`§C3: No ...`）。`bash scripts/check-no-key-leak.sh` 返回 `OK`，exit 0。

- **Python ↔ TS 跨端常量完全一致**: 12 个禁止词（coach/service.py + reflection/service.py 均与 `reports.ts:FORBIDDEN_TONE_WORDS` 字符级完全匹配）；4 个 Persona 名（Python personas.py: Sarah/Marcus/Lin/Daniel ↔ TS personas.ts: structured/pressure/friendly/expert 一一映射）；7 个填充词（Python observer/constants.py `FILLER_WORDS_CN` ↔ TS lib/fillerWords.ts `FILLER_WORDS_CN` 字符级匹配，均含"然后然后"非"然后"，精确）；ACCUSATORY_PREFIXES 4 项（你犯/你又/你总/你居然）；AI_VERDICT_CORE_TERMS 8 项（结构不清晰/缺乏逻辑/偏离主题/证据不足/数据缺失/表达冗长/缺乏深度/条理混乱）— 全部跨端对齐。

- **12 禁止词 + 4 领域护栏层次清晰**: `FORBIDDEN_TONE_WORDS` 在 `reports.ts` 由 `as const` 固化（TypeScript readonly tuple），同文件提供 `scanForbiddenTone` + `sanitizeTone` helper，service 层在 LLM 输出后调用，不依赖 Zod 自动执行（符合 architect §L0-3 规范）。Coach agent 的 `sanitizeCoachOutput` + Reflection agent 的 `sanitizeDiagnosis` / `sanitizeAccusatoryPrefix` 各自有独立的 4 领域 sanitizer，33 + 67 = 100 个教学语气测试全绿。

- **intake_graph 4-edge 顺序链拓扑正确**: START → parse_node → research_node → predict_questions_node → END，严格串行，与设计文档一致。`intakeGraph.optin.test.ts`（8 tests）覆盖 research opt-in/opt-out 两个路径的节点调用计数，opt-out 时 research_node 接收空 company_name 不崩溃。

- **全套 1066 tests / 67 files 全绿，耗时 4.54s**: 较 M2 baseline（307 tests / 726ms）增量显著，无单个失败。TypeScript `--noEmit` 零错误。

---

## 2. 🔴 严重(Critical) — must-fix before M3.4

无 — 9 个 L0 节点名锁全部通过验证，5 维度 / 3 档 / 12 禁止词 / 7 填充词 / 4 Persona / 8 strict() / N=300 PII fuzz / 错误隔离 / 数据契约只增不改 / §C3 零泄漏，全部满足规格。0 个 🔴 严重问题。

---

## 3. 🟡 Minor(Non-blocking) — nice-to-have

- **🟡 turn_graph `next_question_out` 频道名与节点名分离的 JUDGMENT CALL 未回传设计文档**: `turnGraph.ts:32-38` 有 inline JUDGMENT CALL 注释，解释为何状态字段用 `next_question_out` 而非 `next_question`（LangGraph.js 0.2.74 拒绝节点名与状态字段名相同）。该判断调用合理且有注释，但 `.ralph/docs/v34-design/` 中 turn_graph 设计文档可能仍写的是 `next_question` 字段名，存在文档漂移风险。建议 M3.4 arch 在下一次设计文档修订中补充此约束说明（非阻塞）。

- **🟡 intake_graph `INTAKE_GRAPH_NODES` Object.freeze 在注释中提及但未出现在 `Object.freeze(` grep 结果里**: 前序 `grep -E "Object\.freeze\(\s*new Set"` 的结果仅匹配到 `postReportGraph.ts` 和 `intakeGraph.ts` 的注释行（`②  INTAKE_GRAPH_NODES = Object.freeze(new Set([...])) runtime constant`），但实际 `INTAKE_GRAPH_NODES = Object.freeze(` 赋值在第 70 行存在，是 grep 正则 `Object\.freeze\(\s*new Set` 因注释与代码行不同而分别命中不同行的假象。已手工确认 `intakeGraph.ts:70` 和 `intakeGraph.ts:71` 含完整 `Object.freeze(new Set([...]))` 调用。无功能问题，仅命中行与注释行不同导致审计时需人工二次确认，无修复必要。

- **🟡 `postReportGraph.parallel.test.ts` N=8 tests 实际覆盖了 4 个错误路径 + 1 个 WARN 日志 + 2 个并行结构 + 1 个 ZodError 场景，但文件头注释写"4 个错误路径"未数 WARN 日志的 2 个独立 test**: 测试功能完整，注释略有歧义。不影响任何 L0 验证。

- **🟡 `researchAgent.privacy.test.ts` 是独立文件（agent 层 N=300），与 `core/schemas/__tests__/research-privacy.fuzz.test.ts`（schema 层 N=300 + N=50 + N=50）双层覆盖**: 两层 fuzz 重叠但不冗余，agent 层验证的是 `runResearchAgent()` 函数入口，schema 层验证的是 `ResearchAgentInputSchema.parse()` 直调。当前 `pnpm vitest run` 中 `researchAgent.privacy.test.ts` 只显示 1 test（agent 层），schema fuzz 文件有更多 tests。这是正常的分层设计，记录在此以便未来维护者理解。

---

## 4. 整体评分:9/10

M3.3 三图 phase 在 14 个实现节点（3b1ac52..4d4d7ef）中严格执行了所有 L0 约束。9 个节点名字面量精确存在并三层固化；5 维度 / 3 档 / 12 禁止词 / 7 填充词 / 4 Persona 全部字符级匹配；8 个 AgentInputSchema `.strict()` 全部到位；N=300 LCG PII fuzz leakCount=0；Coach/Reflection 真并行 fire-and-forget 契约通过区间重叠测试验证；4 路错误隔离（A/B/C/ZodError）graph.invoke 永不抛出；数据契约只增不改（legacy pass_probability + Verdict 完整保留）；§C3 零秘密过 Bridge；Python ↔ TS 跨端 4 组常量字符级一致。全套 1066 tests / 67 files 全绿，tsc --noEmit clean，check-no-key-leak OK。减 1 分原因：turn_graph 中 `next_question_out` 的 JUDGMENT CALL 属于架构判断，未在设计文档更新中体现，构成轻微文档漂移（🟡 non-blocking）；其余 3 个 🟡 项均为注释/重叠细节，无功能风险。M3.4 streaming ASR phase 可以安全启动。

---

### Verification artifacts

- vitest full suite: 1066 passed / 0 failed (67 files, 4.54s)
- tsc --noEmit: clean (0 errors, 0 warnings)
- check-no-key-leak.sh: OK (exit 0)
- 9 node-name literals: present + Object.freeze ✓
  - turn_graph: "turn_assessment"(L22,207,210,212) / "compression"(L208,211,213) / "next_question"(L209,212,213,214) in turnGraph.ts
  - intake_graph: "parse_node"(L66,268,271,272) / "research_node"(L67,269,272,273) / "predict_questions_node"(L68,270,273,274) in intakeGraph.ts
  - post_report_graph: "coach_node"(L62,167,169,171) / "reflection_node"(L62,168,170,172) in postReportGraph.ts
  - TURN_GRAPH_NODES = Object.freeze(new Set([...])) ✓ (turnGraph.ts:24)
  - INTAKE_GRAPH_NODES = Object.freeze(new Set([...])) ✓ (intakeGraph.ts:70)
  - POST_REPORT_GRAPH_NODES = Object.freeze(new Set([...])) ✓ (postReportGraph.ts:65)
- 4 personas: Sarah↔structured / Marcus↔pressure / Lin↔friendly / Daniel↔expert ✓ (personas.ts:15-18)
- 5 dims: 专业深度/结构化表达/批判性思考/业务直觉/沟通节奏 in DimensionNameSchema ✓ (reports.ts:47-51)
- 3 tiers: PassLikelihoodSchema = z.enum(["中上","中","中下"]) ✓ (reports.ts:37)
- 12 banned words: FORBIDDEN_TONE_WORDS 12-item as const ✓ (reports.ts:63-76)
- 7 filler words: FILLER_WORDS_CN 7-item tuple ✓ (fillerWords.ts:9-17)
- 8 agent input schemas .strict(): ResearchAgentInputSchema(research.ts:80) / CoachAgentInputSchema(coach.ts:33) / ReflectionAgentInputSchema(reflection.ts:71) / FrameworkAgentInputSchema(frameworks.ts:186) / ParseAgentInputSchema(parse.ts:74) / ReferenceAgentInputSchema(turns.ts:101) / ObserverAgentInputSchema(turns.ts:155) / InterviewerAgentInputSchema(turns.ts:220) ✓
- N=300 PII fuzz: leakCount === 0, generateObject never called ✓ (researchAgent.privacy.test.ts 1 test PASS)
- Parallel fan-out (turn_graph): turnGraph.parallel.test.ts 3 tests PASS (interval overlap + sequential call ordering verified)
- Parallel fan-out (post_report_graph): postReportGraph.parallel.test.ts 8 tests PASS (interval overlap ✓ maxStart < minEnd + 20ms jitter)
- Sequential chain (intake_graph): 4-edge topology START→parse→research→predict→END ✓, intakeGraph.optin.test.ts 8 tests PASS
- Error isolation (post_report_graph): graph.invoke never throws ✓ Case A/B/C/ZodError all threw===false (4 tests PASS)
- Contract tests: turnGraph.contract.test.ts 22 PASS / intakeGraph.contract.test.ts 15 PASS / postReportGraph.contract.test.ts 12 PASS
- Agent teaching tests: coachAgent.teaching.test.ts 33 PASS / reflectionAgent.teaching.test.ts 67 PASS / reportAgent.test.ts 116 PASS
- Persona contract: persona.contract.test.ts 24 PASS
- Python ↔ TS parity:
  - 12 forbidden tone words: coach/service.py ↔ reflection/service.py ↔ reports.ts — 字符级 ✓
  - 4 personas: personas.py (Sarah/Marcus/Lin/Daniel) ↔ personas.ts — ✓
  - 7 filler words: observer/constants.py ↔ fillerWords.ts — 含"然后然后"精确匹配 ✓
  - 4 accusatory prefixes: reflection/service.py(你犯/你又/你总/你居然) ↔ reflection/index.ts — ✓
  - 8 AI_VERDICT_CORE_TERMS: reflection/service.py ↔ reflection/index.ts — ✓
- 数据契约只增不改:
  - legacy pass_probability (int 0-100) + summary + reasons + next_actions: 仍在 ReportAgentOutputSchema ✓ (reports.ts:320-323)
  - ReportVerdictSchema(["strong","solid","mixed","weak"]): 仍在 reports.ts:29 ✓
  - 新增字段 pass_likelihood/dimensions/round_reviews_v2/next_actions_v2/ai_verdict/overall_score: nullable/optional 叠加，未删改旧字段 ✓
- §C3 secret 不过 Bridge: grep 无实际 process.env/getApiKey/keychain.read 调用（仅注释标注），check-no-key-leak.sh OK ✓
- v3.4 红线排查: 无 Tauri / Python sidecar / faster-whisper / PyAV / disable-library-validation / network.server — 本 phase 未引入 ✓

---

> 本 audit 不动产品代码，不引入新依赖。
> 唯一写入文件：`.ralph/logs/M3.3.X-audit.md`（tester §B6 明确例外）。
> Auditor: tester (M3.3.X) — 2026-05-05
